# Backend server
NODE_ENV=production
PORT=10000
SERVE_STATIC=false

# Comma-separated list of allowed frontend origins for CORS
# Example: https://systemdesigntracker.vercel.app,https://www.your-domain.com
CORS_ORIGIN=https://systemdesigntracker.vercel.app

# SQLite database file path
# Local example: ./data/activity_guide.db
# Render persistent disk example: /var/data/activity_guide.db
DB_FILE=/var/data/activity_guide.db
