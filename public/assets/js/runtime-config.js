(function () {
  // Set API_BASE_URL to your deployed backend origin, e.g. https://your-service.onrender.com
  // Keep empty only for local same-origin development.
  window.RUNTIME_CONFIG = window.RUNTIME_CONFIG || {
    API_BASE_URL: 'https://your-render-service.onrender.com'
  };
})();
